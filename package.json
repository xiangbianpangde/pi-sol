{
  "name": "pi-sol",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "description": "Pi extension: /sol relays to ChatGPT web GPT-5.6 Sol High via pi-oracle, with Plus High / Power-slider worker patches.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xiangbianpangde/pi-sol.git"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test extensions/__tests__/*.test.ts",
    "check:patches": "node extensions/lib/sol/apply-sol-patches.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fs-ext": "^2.1.1"
  }
}
